spring:
  application:
    name: tondo
  datasource:
#    url: jdbc:mysql://localhost:3306/tondo?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://localhost:3306/tondo?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

mybatis-plus:
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: TondoSecretKey2026YourVeryLongRandomStringHereAtLeast256BitsChangeThis
  expiration: 604800000

minio:
  endpoint: http://localhost:9090
  access-key: minioadmin
  secret-key: minioadmin
  bucket: tondo